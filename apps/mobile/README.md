# apps/mobile

Reserved for the Expo (React Native) app per [ADR-0001](../../docs/specs/ADR/0001-adopt-expo-reject-pwa.md).

Will be initialized in a future PR (PRD-0008 / TDD-0008 onwards). Empty placeholder
exists so the monorepo workspace layout matches what the specs assume.
